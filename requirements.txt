# 1.56.0 bundles chromium r1194 / ffmpeg r1011, which is what the Claude Code
# web containers ship prebuilt in /opt/pw-browsers. 1.58 wants chromium r1208,
# so every run there died at launch asking for `playwright install` — which
# those containers cannot do. If you bump this, check the new chromium
# revision against that image, or set BROWSER_EXECUTABLE_PATH (see
# poms/shared/driver.py) to point at whatever browser is actually present.
playwright==1.56.0
python-dotenv>=1.0.0
PyYAML==6.0.3
pytest==9.0.2
pytest-asyncio==1.3.0
allure-pytest==2.15.3
allure-python-commons==2.15.3
anthropic==0.86.0
openai==2.31.0
groq==1.1.2
google-generativeai==0.8.6
sentence-transformers==5.4.0
numpy==2.4.2
Pillow==12.1.0
