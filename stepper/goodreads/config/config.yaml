base_url: "https://www.goodreads.com"
headless: true
slow_mo_ms: 200
browser: "chromium"

# Paths (relative to workspace root)
screenshots_dir: "artifacts/screenshots"
storage_state_path: "artifacts/storage_state.json"

# Rate limiting & delays (milliseconds)
delays:
  page_load_wait_ms: 1000
  between_actions_ms: 300
  between_pagination_ms: 1000
  post_login_wait_ms: 1000
  retry_backoff_ms: 2000
  search_submit_delay_ms: 500
