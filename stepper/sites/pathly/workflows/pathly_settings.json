{
  "name": "pathly-settings-check",
  "description": "Navigate to Settings panel, read current routing engine value, and screenshot. Read-only — does not mutate developer config.",
  "variables": {},
  "steps": [
    { "action": "pathly_navigate_panel", "description": "Navigate to settings panel", "extra": { "panel": "settings" } },
    { "action": "pathly_read_routing", "description": "Read current routing engine value (read-only)" },
    { "action": "screenshot", "description": "Screenshot settings panel" }
  ]
}
